<Window x:Class="GuiBuddy.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GuiBuddy.App"
        mc:Ignorable="d"
        Title="GuiBuddy Debug" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding StatusMessage.Value}" FontSize="14" Margin="0,0,0,10"/>

        <ListBox Grid.Row="1" ItemsSource="{Binding Windows}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Handle}" Width="100" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Title}" Margin="10,0,0,0"/>
                        <TextBlock Text="{Binding Bounds}" Margin="10,0,0,0" Foreground="Gray"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Button Grid.Row="2" Content="Refresh Windows" Command="{Binding RefreshCommand}" 
                Height="30" Margin="0,10,0,0"/>
    </Grid>
</Window>

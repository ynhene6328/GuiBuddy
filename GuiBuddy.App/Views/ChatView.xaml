<UserControl x:Class="GuiBuddy.App.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Window Selection Trigger -->
            <RowDefinition Height="*"/>    <!-- Chat Log -->
            <RowDefinition Height="Auto"/> <!-- Input Area -->
        </Grid.RowDefinitions>

        <!-- Window Selection Area -->
        <Border Grid.Row="0" Padding="10" Background="#F0F0F0" BorderBrush="#DDDDDD" BorderThickness="0,0,0,1">
            <StackPanel>
                <TextBlock Text="Target Window Selection" FontWeight="Bold" Margin="0,0,0,5"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox Grid.Column="0" 
                              ItemsSource="{Binding AvailableWindows}" 
                              SelectedItem="{Binding SelectedTargetWindow.Value}"
                              DisplayMemberPath="Title"
                              Margin="0,0,5,0"
                              Padding="4"/>
                    <Button Grid.Column="1" Command="{Binding RefreshWindowsCommand}" Content="🔄" Width="30" Margin="0,0,5,0" ToolTip="Refresh Windows"/>
                    <Button Grid.Column="2" Command="{Binding ConfirmTargetCommand}" Content="Confirm" Padding="10,0"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Chat Log -->
        <ListBox ItemsSource="{Binding Messages}" 
                 Grid.Row="1" 
                 BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="5" Background="#F0F0F0" Padding="8" Margin="5">
                        <StackPanel>
                            <TextBlock Text="{Binding Sender}" FontWeight="Bold" Foreground="#0078D7" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding Text}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <!-- Input Area -->
        <DockPanel Grid.Row="2" Margin="5">
            <Button Content="Send" DockPanel.Dock="Right" Command="{Binding SendCommand}" Width="60" Margin="5,0,0,0"/>
            <TextBox Text="{Binding InputText.Value, UpdateSourceTrigger=PropertyChanged}" 
                     Height="60" 
                     AcceptsReturn="True" TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"/>
        </DockPanel>
    </Grid>
</UserControl>

<Window x:Class="GuiBuddy.App.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:GuiBuddy.App.ViewModels"
        mc:Ignorable="d"
        Title="設定" Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="AI連携設定" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Text="Gemini APIキー:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="2" Text="{Binding ApiKey.Value, UpdateSourceTrigger=PropertyChanged}" 
                 VerticalAlignment="Top" Padding="5"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="キャンセル" Command="{Binding CloseCommand}" Width="80" Margin="0,0,10,0"/>
            <Button Content="保存" Command="{Binding SaveCommand}" Width="80"/>
        </StackPanel>
    </Grid>
</Window>

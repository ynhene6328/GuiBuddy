## TODO

- 最前面表示ウィンドウが切り替わったとき、対象ウィンドウをそれに変更するかを確認する（プロセスIDの異なる別ウィンドウを立ち上げるケースがある）

- ウィンドウ選択が切り替わったとき、Systemからのメッセージとして表示されない、要動作検証

- 目標が達せられた場合に、UI要素監視を停止したい、ただし、AIの判断で完了としてもユーザが求める答えと違う場合は、再開する可能性がある、これを考慮すること

- オーバーレイウィンドウの左上の文字列表示不要か、削除するかAIからのメッセージを表示する等検討する

- オーバーレイウィンドウのデザインの変更を検討する（角を取る、色を変える等）

- AIのモデルによって、回答に差があるが、低レベルのモデルだと、UI要素のIDをユーザ向けに提示するケースがある、UI要素IDはユーザは知らないため使わせないようにプロンプトに追記する

- AIの使用可能モデルを整理、確認する

- 無償モデルの使用上限に到達した時、別のモデルを使用することをユーザに提示する、もしくは自動切換えするなど、レベルアップアイテムとして検討する

- 設定ウィンドウのデザインをChatViewに合わせて修正する、新規ウィンドウではなく、MainWindow内に表示することも併せて検討する

- UIAが取得できないアプリケーションについて、OCRで簡易版UI要素取得ツールを自力で組むことを検討

- 対象アプリケーションのマニュアルが存在する場合、それを読み込ませることが出来るか検討する（小規模ツールでreadme.mdが存在するケース等、これを読み込ませることでガイドが可能になる可能性がある）

- アイコンを検討する

